plugins {
    id "java"
    id "maven"
    id "io.freefair.maven-jars" version "0.9.10"
}

archivesBaseName = "okhttp2-spring-boot-starter"

dependencies {
    compile project(':autoconfigure')
    compile 'com.squareup.okhttp:okhttp:2.+'
}