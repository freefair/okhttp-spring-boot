plugins {
    id "java"
    id "maven"
    id "io.freefair.maven-jars" version "0.9.7-maven"
}

archivesBaseName = "okhttp-spring-boot-starter"

dependencies {
    compile project(':autoconfigure')
    compile 'com.squareup.okhttp3:okhttp:3.5.0'
}